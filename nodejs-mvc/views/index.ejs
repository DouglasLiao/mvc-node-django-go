<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .hero { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 4rem 0; }
        .card { box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,.075); border: 1px solid rgba(0,0,0,.125); }
    </style>
</head>
<body>
    <!-- Navegação -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">🚀 MVC Node.js</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link" href="/users">Usuários</a>
                <a class="nav-link" href="/users/create">Criar Usuário</a>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero text-center">
        <div class="container">
            <h1 class="display-4 fw-bold"><%= message %></h1>
            <p class="lead">Este é um exemplo prático de arquitetura MVC (Model-View-Controller) usando Node.js e Express</p>
            <a href="/users" class="btn btn-light btn-lg">Ver Usuários</a>
        </div>
    </section>

    <!-- Conteúdo -->
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <div class="card-body text-center">
                        <h5 class="card-title">📊 Model</h5>
                        <p class="card-text">Gerencia os dados e a lógica de negócio. No nosso exemplo, a classe User.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <div class="card-body text-center">
                        <h5 class="card-title">👁️ View</h5>
                        <p class="card-text">Interface do usuário. Arquivos EJS que renderizam HTML dinâmico.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <div class="card-body text-center">
                        <h5 class="card-title">🎮 Controller</h5>
                        <p class="card-text">Controla o fluxo da aplicação, conectando Model e View.</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <h5>🚀 Como funciona o MVC?</h5>
                    </div>
                    <div class="card-body">
                        <ol>
                            <li><strong>Usuário faz uma requisição</strong> → O Express recebe via rotas</li>
                            <li><strong>Rota chama o Controller</strong> → UserController processa a requisição</li>
                            <li><strong>Controller usa o Model</strong> → Busca/modifica dados no User</li>
                            <li><strong>Controller renderiza a View</strong> → EJS gera HTML com os dados</li>
                            <li><strong>Response é enviada</strong> → Usuário vê o resultado</li>
                        </ol>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-light text-center py-3 mt-5">
        <div class="container">
            <p>&copy; 2024 MVC Node.js Example - Aprendendo Node.js com MVC</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>